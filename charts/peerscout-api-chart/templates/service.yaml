apiVersion: v1
kind: Service
metadata:
  name: {{ include "peerscout-api-chart.fullname" . }}
  labels:
    {{- include "peerscout-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "peerscout-api-chart.selectorLabels" . | nindent 4 }}
