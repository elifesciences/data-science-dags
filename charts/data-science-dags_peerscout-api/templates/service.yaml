apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-science-dags_peerscout-api.fullname" . }}
  labels:
    {{- include "data-science-dags_peerscout-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-science-dags_peerscout-api.selectorLabels" . | nindent 4 }}
