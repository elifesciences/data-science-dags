apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-science-dags_peerscout-api.fullname" . }}-test-connection"
  labels:
    {{- include "data-science-dags_peerscout-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-science-dags_peerscout-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
